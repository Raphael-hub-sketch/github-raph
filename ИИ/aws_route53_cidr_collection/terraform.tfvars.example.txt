# terraform.tfvars.example
# Пример заполнения — скопируйте в terraform.tfvars

aws_region   = "us-east-1"
environment  = "production"
project_name = "cidr-routing"

# Для production рекомендуется указывать имя явно
cidr_collection_name = "mycompany-production-cidr"

locations = {
  "us-east-nyc" = {
    cidr_blocks = ["10.0.0.0/24"]
  }
  "us-west-sfo" = {
    cidr_blocks = ["10.1.0.0/24"]
  }
}